# Compatibility pointer for the ICASSP 2027 environment locks.
#
# Create a Python 3.11 environment, install environments/cpu.lock.txt on CPU
# hosts, or install the self-contained environments/gpu.lock.txt on the Linux
# single-GPU training host. Do not layer the GPU lock over the CPU environment.
# Exact interpreter, package, CUDA, driver and hardware fingerprints are also
# embedded in every ResultEnvelope.  GPU reproduction is tolerance-based; this
# project does not claim bitwise identity across devices or driver revisions.
-r environments/cpu.lock.txt
